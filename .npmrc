registry=https://registry.npmmirror.com

# 允许所有包的 pre/post install 脚本运行
enable-pre-post-scripts=true

# 允许 @vscode/ripgrep 的 build 脚本
# pnpm 会忽略 build scripts，需要明确允许
# 用户安装时会自动运行 postinstall 下载 ripgrep 二进制

# 解决 unicorn-magic 等包的 exports 问题
public-hoist-pattern[]=*unicorn-magic*
